PORT=3001
JWT_SECRET=your-secret-key-change-in-production

# Optional: use Postgres for persistent storage (e.g. on Render). If unset, SQLite is used (local file).
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# SQLite path (only when DATABASE_URL is not set)
DATABASE_PATH=./data/todos.sqlite
